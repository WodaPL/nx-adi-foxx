import { Wrapper } from '../../styles/showcase-points-trapezoid/showcase-points-trapezoid';

export const ShowcasePointsTrapezoid = ({
  id,
  className,
}: {
  id?: string;
  className?: string;
}) => {
  return <Wrapper id={id}>showcase points</Wrapper>;
};

export default ShowcasePointsTrapezoid;
